/*
 * ActionBlock Messages
 *
 * This contains all the text for the ActionBlock component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ActionBlock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ActionBlock component!',
  },
});
